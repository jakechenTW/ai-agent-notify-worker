{
  "name": "ai-agent-notify-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.0.0"
  },
  "dependencies": {
    "@gramio/format": "^0.9.0",
    "marked": "^15.0.12"
  }
}
